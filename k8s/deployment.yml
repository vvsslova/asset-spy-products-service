apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: asset-spy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: vvsslova/asset-spy-product-service:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "150m"
            limits:
              memory: "384Mi"
              cpu: "300m"
          envFrom:
            - configMapRef:
                name: products-service-configmap
            - secretRef:
                name: products-service-secret
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms256m -Xmx384m"
          ports:
            - containerPort: 8081